import React from 'react';

const Allsubmissioncard = ({item}) => {
const{file,description,examinee ,examineeemail,type,author,totalmark,assignmentname}=item
console.log(author);

  return (
    <div>
      
    </div>
  );
};

export default Allsubmissioncard;